# AgentHive — per-worktree agent environment
# Generated by 007-agent-security-roles setup
# DO NOT commit secrets; fill in passwords before use.

AGENT_WORKTREE=copilot-one
AGENT_PROVIDER=copilot
AGENT_BRANCH=copilot/one
AGENT_DB_USER=agent_copilot_one
AGENT_DB_PASSWORD=CHANGE_ME_agent_copilot_one

# Derived connection string (override if needed)
DATABASE_URL=postgresql://${AGENT_DB_USER}:${AGENT_DB_PASSWORD}@localhost:5432/agentdemo

# Git identity (used by agent-spawner when forking)
GIT_CONFIG_GLOBAL=/data/code/AgentHive/.git/worktrees-config/copilot-one.gitconfig
GIT_CONFIG_NOSYSTEM=1
