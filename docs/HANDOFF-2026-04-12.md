# AgentHive Session Handoff — 2026-04-12

## Current State

### Services Running
- ✅ MCP Server (port 6421) — use `curl http://127.0.0.1:6421/health` to verify
- ✅ PipelineCron — systemd service `hermes-gate-pipeline.service`
- ✅ Orchestrator — systemd service `hermes-orchestrator.service`
- ✅ Discord Bridge — systemd service `hermes-discord-bridge.service`
- ✅ Hermes Gateway — systemd service `hermes-gateway.service`

### Database
- Schema: `roadmap` (configured via `PG_SCHEMA=roadmap` in `~/.agenthive.env`)
- Migrations applied: 002-007, 012-013, 015-016
- Migration needed: 017 (daily efficiency views)

### State Machine Status
- 100+ proposals in system
- DRAFT: ~8 proposals
- REVIEW: ~12 proposals  
- DEVELOP: ~4 proposals
- COMPLETE: ~18 proposals
- DEPLOYED: ~22 proposals

## Critical Issues

### 1. Proposal Create Broken
**Error:** "window functions are not allowed in FILTER"
**Location:** `src/infra/postgres/proposal-storage-v2.ts:239`
**Problem:** SQL query uses window function inside FILTER clause
**Fix needed:** Rewrite query to avoid window function in FILTER

### 2. Roadmap Overview Ignores Schema
**Error:** Queries public schema instead of roadmap schema
**Impact:** Shows wrong statistics
**Workaround:** Use `roadmap proposal list --plain`

### 3. Token Tracking Not Automatic
**Status:** `metrics.token_efficiency` table exists
**Issue:** Agents don't automatically log token usage
**Need:** Add automatic logging to orchestrator dispatch

## Pending Proposals (HIGH priority)

- P191: Daily efficiency views
- P194: Project memory & agent memory system
- P195: Enhanced token tracking with budget circuit breaker
- P196: Cubic lifecycle management and idle cleanup

## Agent System

### Dispatch Map
```
DRAFT → Architect + Researcher
REVIEW → Reviewer + Skeptic Alpha + Skeptic Beta
DEVELOP → Developer + Test Mirror
MERGE → Git Specialist + E2E Orchestrator
```

### Active Agents
- Orchestrator (dispatches on state changes)
- PipelineCron (processes transition queue)
- Skeptic Alpha (adversarial reviewer)
- Skeptic Beta (implementation quality)
- System Monitor (consistency checker)

## Commands

### Start Services
```bash
systemctl --user start hermes-gate-pipeline.service
systemctl --user start hermes-orchestrator.service
systemctl --user start hermes-discord-bridge.service
```

### Check Status
```bash
systemctl --user status hermes-gate-pipeline.service
systemctl --user status hermes-orchestrator.service
curl http://127.0.0.1:6421/health
```

### View Proposals
```bash
cd /data/code/AgentHive
node --import jiti/register src/apps/cli.ts proposal list --plain
```

### Apply Migration
```bash
psql $DATABASE_URL -f database/ddl/017-daily-efficiency-views.sql
```

## Next Steps

1. Fix SQL bug in proposal-storage-v2.ts (line 239)
2. Apply migration 017 (daily efficiency views)
3. Add automatic token logging to orchestrator
4. Implement cubic cleanup (P196)
5. Implement project memory system (P194)

## Discord
- State changes posted to AI #general channel (ID: 1480366428325548200)
- A2A messages bridged to Discord
- Agent activity visible in real-time
